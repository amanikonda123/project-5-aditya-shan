import processing.core.PImage;

import java.util.List;

public class Hospital extends Entity {

    public Hospital(String id, Point position, List<PImage> images, int imageIndex) {
        super(id, position, images, imageIndex);
    }
}
